# Services

Integration boundaries live here: HTTP/API clients, authentication transport, storage adapters, and other external-system clients. UI components should not contain raw Axios configuration or duplicated API setup.
