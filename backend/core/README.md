# Core

Cross-cutting backend infrastructure belongs here: authentication primitives, permissions shared across domains, exception handling, middleware, response helpers, validators, pagination, and constants.

Business-domain models and workflows must remain in `backend/apps/`.
